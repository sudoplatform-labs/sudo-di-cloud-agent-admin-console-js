import styled from 'styled-components';

/**
 * A div that expands to fill up its parent
 */
export const StretchDiv = styled.div`
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
`;
